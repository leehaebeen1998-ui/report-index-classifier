[project]
name = "report-index-classifier"
version = "0.1.0"
description = "Reusable report index classification module"
requires-python = ">=3.10"
dependencies = ["openpyxl>=3.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
